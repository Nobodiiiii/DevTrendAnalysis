下面这套说明会严格围绕“**给出建议**”来设计：你需要哪些字段（以及它们的**详细可取值/标准化口径**）、怎么清洗、怎么生成基准表（只查 gold）、以及在线如何用基准表输出建议。

---

## 1) 你需要的“详细属性值口径”与标准化规则

你要做的基准表目标是：**给用户回答**

* “你在同类人群（cohort）里薪资处于什么水平？”
* “下一档（比如 P75）的人通常具备哪些共同特征？”
* “你应该优先优化哪些变量（可控/半可控）？”

为此必须把字段统一成**稳定、可分组、可回退**的形式。

### 1.1 薪资与货币（核心指标）

**必须字段**

* `ConvertedCompYearly`（double）

  * 单位：统一成“年收入（同一币种，通常 USD）”
  * 清洗规则：

    * 为空或 <=0：丢弃（不参与薪资基准统计）
    * 极端值：可做 winsorize 或按国家分组的 IQR/分位数裁剪（建议先裁到 \[p0.5, p99.5]）以防异常填报扭曲分位数
  * 输出用途：基准表的 p25/p50/p75/p90 与用户定位

**可选字段（用于质量检查/调试）**

* `CompTotal`（double）+ `Currency`（string）

  * 你如果能稳定维护汇率与频率（年/月/周），可以用来验证 ConvertedCompYearly 的合理性
  * 不建议第一版依赖它去算 ConvertedCompYearly（除非你确认数据一致）

### 1.2 cohort 维度（用于对标：越稳定越好）

以下维度建议都做“**标准化字段**”，并最终用于 gold 表分组：

#### A) 地域

* `Country`（string）

  * 标准化：ISO 国家名/代码（建议用一个维表映射）
  * 用法：这是最重要的分组维度之一（薪资强相关）

#### B) 雇佣状态

* `Employment`（string）

  * 标准化建议：归并到有限集合（便于稳定统计）：

    * `employed_full_time` / `employed_part_time` / `self_employed` / `student` / `unemployed` / `other`
  * 用法：全职/非全职薪资差异非常大

#### C) 经验

你列的目标维度里有 `WorkExp` 和 `YearsCode`，建议这样处理：

* `WorkExp`（string 或 numeric）

  * 目标：得到一个数值 `WorkExpYears`（int/float）
  * 再分箱：`WorkExpBin`

    * 推荐分箱：`0-1`, `2-3`, `4-6`, `7-10`, `11-15`, `16+`
* `YearsCode`（string，可能带文本）

  * 目标：得到 `YearsCodeNum`
  * 清洗逻辑要能处理：`Less than 1 year` / `More than 50 years` / `NA`
  * 用法：可作为辅助维度或在 WorkExp 缺失时回退使用

> 为什么要分箱：为了稳定分位数统计 + 防止 cohort 过细导致 n 太小。

#### D) 岗位类型（多选，最容易脏）

* `DevType`（`;` 分割多选）

  * Silver 里你必须存两份：

    1. `DevType_raw`（原字符串）
    2. `DevType_tokens`（array<string>，规范化 token）
  * 你还需要一个：`DevType_primary`（string）

    * 目的：让 cohort 可分组（否则多选会爆炸）
    * 推荐主项选择规则（从易到难）：

      1. **规则优先级表**（你维护一个 rank 列表，稳定且可解释）
      2. 若不想手工维护：用 2025 年数据算每个 token 的“样本覆盖率”，取用户 token 中最常见/最稳定的作为 primary（减少稀有 token 引发的小样本问题）
      3. 高阶：用与薪资相关性（lift/均值差）选“最影响薪资的 token”做 primary（更贴近建议，但实现更复杂）

#### E) 工作方式

* `RemoteWork`（string）

  * 标准化建议：

    * `remote` / `hybrid` / `in_person` / `other_or_unknown`
  * 用法：对薪资与岗位分布影响明显

#### F) 组织规模

* `OrgSize`（string）

  * 标准化建议：映射为可排序的档位（并可做更粗回退）

    * `1-9`, `10-19`, `20-99`, `100-499`, `500-999`, `1000-4999`, `5000-9999`, `10000+`, `unknown`
  * 用法：对薪资影响较大，但容易分太细导致 n 不足，务必支持回退（比如合并成 small/medium/large）

#### G) 角色层级

* `ICorPM`（string）

  * 标准化：

    * `ic`（individual contributor）
    * `pm_people_manager`（people manager）
    * `pm_project_manager`（project manager，如果数据有）
    * `unknown`
  * 用法：极其关键，同年同岗同国家的薪资差往往很大

---

## 2) “围绕建议”的输出：你需要的基准表字段（Gold schema）

你不是为了报表而报表，你要的是“能回答用户问题”的最小集合。建议至少产出下面这张核心表：

### 2.1 核心薪资基准表（必须）

表名示例：`gold_salary_benchmark_2025`

**主键（cohort key）**

* `year`（如果你未来要多年来回切换）
* `Country_std`
* `Employment_std`
* `WorkExpBin`（或 `WorkExpYears` 但不建议）
* `DevType_primary`
* `RemoteWork_std`
* `OrgSize_bin`（建议用粗 bins）
* `ICorPM_std`

**指标列**

* `n`（样本量）
* `p25`, `p50`, `p75`, `p90`（使用 `percentile_approx`）
* （可选）`mean`, `std`，以及 `min/max`（用于监控）
* （可选）`currency_base`（如果 ConvertedCompYearly 是统一币种，可以标识）

**为什么这些列足够做建议**

* 你能告诉用户：你属于哪个 cohort、这个 cohort 的薪资分布、你处于哪个分位
* 你能做回退：当 n 太小就去掉某些维度重新查（见后面流程）

### 2.2 用于“下一档怎么做”的辅助基准表（强烈建议）

因为只有分位数还不够“建议”，你还需要告诉他：**高薪组的共同特征**是什么。第一版可以做“维度层面的 uplift”，不涉及技能（技能建议你以后会用 Have/Want 做）。

表名示例：`gold_feature_uplift_2025`

**分组维度**：同上 cohort，但可以更粗一些（避免太稀）

* `Country_std`, `Employment_std`, `WorkExpBin`, `DevType_primary`（保留）
* Remote/OrgSize/ICorPM 可以作为“被比较的特征”而不是 cohort 维度（减少稀疏）

**比较逻辑**

* 在每个粗 cohort 中：

  * 先用 `ConvertedCompYearly` 算 `p75`
  * 定义高薪组：`salary >= p75`
  * 统计高薪组 vs 全体在某些特征上的占比差（uplift / lift）

    * 例如：RemoteWork=remote 的比例在高薪组高多少
    * ICorPM=PM 的比例在高薪组高多少
    * OrgSize=large 的比例在高薪组高多少

**输出列**

* `feature_name`（如 `RemoteWork_std`）
* `feature_value`（如 `remote`）
* `coverage_all`, `coverage_high`, `uplift = coverage_high - coverage_all`
* `n_all`, `n_high`

这张表能直接生成“建议话术”：

* “在与你类似的人群中，进入 P75 的人更常见的工作方式是 X（uplift +12%）……”

---

## 3) 详细流程：从清洗到建议输出（不写代码，但把每一步做什么讲透）

### 3.1 离线：Raw → Silver（清洗标准层）

**输入**：`main.survey_results_2025`（原始表）
**输出**：`silver_survey_2025_clean`（Parquet/Delta）

Silver 必须做的事情（这是建议系统能否稳跑的关键）：

1. **列重命名/规范化**（特别是有空格/特殊字符的列，2025 里你已经见到了）
2. **类型转换**

   * `ConvertedCompYearly` cast double
   * `CompTotal` cast double
3. **经验字段解析**

   * `WorkExp` → `WorkExpYears` + `WorkExpBin`
   * `YearsCode` → `YearsCodeNum`（可选）+ `YearsCodeBin`（可选）
4. **DevType 多选拆分**

   * `DevType_raw`
   * `DevType_tokens`（array 去空、trim、去重）
   * 生成 `DevType_primary`（按你确定的规则）
5. **枚举值标准化**

   * `Employment_std`, `RemoteWork_std`, `ICorPM_std`, `OrgSize_bin`, `Country_std`

> 注意：Silver 层要保留 `ResponseId` 以便你将来做抽样、解释、相似人群等。

### 3.2 离线：Silver → Gold（基准表）

**输入**：`silver_survey_2025_clean`
**输出**：`gold_salary_benchmark_2025`（以及可选 uplift 表）

步骤：

1. 过滤有效薪资样本（ConvertedCompYearly not null and >0）
2. 按 cohort key groupBy，算：

   * `n`
   * `percentile_approx` 得到分位数
3. 写入 Parquet/Delta，并按常用维度分区（至少按 `Country_std` / `WorkExpBin` / `DevType_primary` 之一分区，利于快速读取）

### 3.3 在线：用户 JSON → cohort → 只查 Gold → 输出建议

**在线不允许扫粗数据**，所以在线流程必须是：

1. 前端 JSON（或后端组装）提供这些最小字段：

   * `Country`、`Employment`、`WorkExpYears` 或 `WorkExpBin`、`DevType_tokens`（或 `DevType_primary`）、`RemoteWork`、`OrgSize`、`ICorPM`
   * （可选）用户当前薪资（如果要算“你在分位中位置”）
2. 后端对用户字段做同样标准化（必须与 Silver 逻辑一致）
3. 生成 cohort key
4. 查询 `gold_salary_benchmark_2025` 得到分位数和 n
5. **回退策略（必须）**：如果 `n < threshold`（比如 <200）

   * 回退 1：去掉 `OrgSize_bin`
   * 回退 2：去掉 `RemoteWork_std`
   * 回退 3：DevType\_primary 换成更粗分类（例如 DevType\_family）
   * 回退 4：WorkExpBin 合并更粗（例如 `0-3`, `4-10`, `11+`）
   * 最后仍不足：只按 `Country + Employment + WorkExpBin` 做对标
6. 生成建议文本（核心三段）

   * **薪资对标**：P50/P75 与用户对比（若用户提供）
   * **提升方向**：来自 uplift 表（或你后续技能表）
   * **可信度说明**：cohort 样本量 n、以及你使用了哪个回退层级（透明很重要）

---

## 4) 你需要“详细属性值”的清单（总结版）

围绕你现在的目标（薪资基准表 + cohort 对标），你需要用户/数据里最终能落到这些标准字段：

### 用户画像（在线必需）

* `Country_std`：国家（标准化）
* `Employment_std`：雇佣状态（标准化）
* `WorkExpBin`：工作经验分箱（或 WorkExpYears）
* `DevType_tokens` + `DevType_primary`：岗位类型 token + 主项
* `RemoteWork_std`：远程/混合/线下
* `OrgSize_bin`：组织规模档位
* `ICorPM_std`：IC/Manager
* （可选）`user_salary_yearly`：用户薪资（用于定位分位，否则只能给 cohort 分布）

### 训练/统计（离线必需）

* 以上所有字段 + `ConvertedCompYearly`
* `ResponseId`（用于审计、去重、可追溯）

---

## 5) 建议系统的“话术结构”应该怎么长（保证围绕建议）

你最终返回给前端的建议，建议固定成结构化三段（你现在 UI 也分了“数据分析建议 / AI建议”）：

### 数据分析建议（严格基于 Gold）

* 你的 cohort 定义：Country/Employment/WorkExpBin/DevType\_primary/RemoteWork/OrgSize/ICorPM
* 样本量 n
* 分位数 p25/p50/p75/p90
* 用户相对位置（若提供薪资）
* 回退信息（若发生）

### 行动建议（基于 uplift，或你后续技能差距）

* “进入 P75 的人更常见的可控特征是什么”（例如 manager 比例、远程比例、公司规模倾向）
* 用 uplift 排序给 Top3（带 uplift 数字和样本量 n\_high）

---

如果你按这个方案推进，下一步最关键的决策只有两个（不需要你现在回答，我只是点明“你最终必须定下来”的口径）：

1. `DevType_primary` 的选择规则（决定 cohort 是否稳定）
2. `n 阈值 + 回退顺序`（决定建议是否总能给出、且可信度如何）

你后面只要把这两条口径定死，我就能把整个“Silver 清洗字段规范”与“Gold 基准表设计 + 回退策略”进一步细化到可直接交给工程实现的程度（仍然可以先不写代码）。
